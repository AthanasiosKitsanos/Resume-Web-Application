@addTagHelper*, Microsoft.AspNetCore.Mvc.TagHelpers
@using System.Security.Claims
@inject ResumeProject.Services.JwtTokenService jwtTokenService
@{  
    var refreshedToken = await jwtTokenService.RefreshAccessTokenAsync();
}

<!DOCTYPE html>

<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>@ViewData["Title"] - Resume Web Application</title>
        <link rel="stylesheet" href="~/styles.css" asp-append-version="true">
        @RenderSection("Styles", required: false) 
    </head>
    <body>
        <header>
            <nav class="nav-container">
                <ul class="nav-list">

                    <li class="nav-item">
                        <a href="/">Home</a>
                    </li>
                    @if(User.Identity is not null && User.Identity.IsAuthenticated)
                    {
                        var firstName = User.FindFirst("FirstName")?.Value;
                        var lastName = User.FindFirst("LastName")?.Value;
                        var fullName = (!string.IsNullOrEmpty(firstName) && !string.IsNullOrEmpty(lastName)) ? $"{firstName} {lastName}" : User.Identity.Name;
                        <li>
                            <a href="/Account/Update">Update Info</a>
                        </li>

                        <li>
                            <a href="/Account/Logout">Logout</a>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item dropdown">
                                <a href="">Account</a>
                                <ul class="dropdown-menu">
                                    <li>
                                        <a href="/Account/Register">Register</a>
                                    </li>

                                    <li>
                                        <a href="/Account/Login">Login</a>    
                                    </li>
                                </ul>
                        </li>
                    }
                </ul>   
            </nav>
        </header>
        
        <main class="container">
            @RenderBody()
        </main>

        <footer>
            
        </footer>
    </body>
</html>